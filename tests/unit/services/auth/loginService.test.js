const LoginService = require('../../../../services/auth/loginService');

describe('LoginService', () => {
  test.todo('should login user with valid credentials');
  test.todo('should throw UnauthorizedError with invalid credentials');
});
