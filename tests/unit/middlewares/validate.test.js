const { validate, validateLogin, validateRegister } = require('../../../validators/validate');

describe('Validate Middleware', () => {
  test.todo('should reject invalid login payload');
  test.todo('should reject invalid register payload');
});
