const SessionController = require('../../../../controllers/auth/sessionController');

describe('SessionController', () => {
  test.todo('logout should invoke service and return 200');
  test.todo('getSessions should invoke service and return session list');
  test.todo('logoutAll should invoke service and return 200');
});
